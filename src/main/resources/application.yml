server:
  port: 33001
spring:
  application:
    name: service-config-server
  cloud:
    config:
      server:
        git:
          uri: https://gitlab.com/pqab-ms/repo-config.git
          username: config
          password: xA86jvagwDjFDVRXSxxm